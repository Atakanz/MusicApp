import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const LikedSongs = () => (
	<SafeAreaView>
		<Text>
        Hello LikedSongs!
		</Text>
	</SafeAreaView>
);

export default LikedSongs;
